---
// CTA Sticky bar — appears on scroll
---

<div class="cta-sticky" id="cta-sticky">
  <p>📞 Instalação <strong>Custo Zero</strong> para seu condomínio</p>
  <a href="/#contato" class="btn btn-primary" id="cta-sticky-btn" style="padding: 12px 28px; min-height: 44px; font-size: 0.9rem;">
    Solicitar Projeto
  </a>
</div>

<script>
  const ctaSticky = document.getElementById('cta-sticky');
  if (ctaSticky) {
    let lastScroll = 0;
    window.addEventListener('scroll', () => {
      const scrollY = window.scrollY;
      if (scrollY > 600 && scrollY > lastScroll) {
        ctaSticky.classList.add('visible');
      } else if (scrollY < 300) {
        ctaSticky.classList.remove('visible');
      }
      lastScroll = scrollY;
    }, { passive: true });
  }

  const ctaStickyBtn = document.getElementById('cta-sticky-btn');
  if (ctaStickyBtn) {
    ctaStickyBtn.addEventListener('click', () => {
      if (window.dataLayer) {
        window.dataLayer.push({ event: 'cta_click', click_location: 'sticky_bar' });
      }
    });
  }
</script>
